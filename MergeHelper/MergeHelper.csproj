<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>netstandard1.1;netstandard2.0;net45;net461</TargetFrameworks>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Authors>David West</Authors>
    <Company />
    <Description>An arsenal of extension methods for merging in-memory collections : add, update, delete elements simultaneously.  Supports a functional LINQ approach (Merge), in-place collection merging (MergeInto), and a pure event-driven approach (ControlMerge, ControlMergeParallel).  Many variations exist to allow custom behaviors and special cases.</Description>
    <Copyright>© 2018 David West</Copyright>
    <PackageProjectUrl>https://github.com/davidwest/MergeHelper</PackageProjectUrl>
    <RepositoryUrl>https://github.com/davidwest/MergeHelper</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>merge collection enumerable linq</PackageTags>
    <PackageIconUrl>https://i.postimg.cc/13BJFrH7/merge.png</PackageIconUrl>
    <NeutralLanguage>en</NeutralLanguage>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.Threading.Tasks.Parallel" Version="4.3.0" />
  </ItemGroup>

  <ItemGroup>
    <Reference Update="System.Runtime.Serialization">
      <SpecificVersion>true</SpecificVersion>
    </Reference>
  </ItemGroup>

</Project>
